# Hetzner VPS — copy to hetzner/config.env (gitignored) or export before deploy
HETZNER_IP=167.233.120.228
SSH_KEY=$HOME/.ssh/hetzner
# Optional: override OpenAI key for server (otherwise read from cloudflare/.dev.vars or app/.env)
# OPENAI_API_KEY=sk-...
# Set to 0 to skip Cloudflare Worker redeploy
DEPLOY_WORKER=1
