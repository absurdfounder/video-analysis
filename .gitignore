# Local JSON fallback for website sync (use Netlify Blobs in production)
app/data/project.json

# Cloudflare Worker
cloudflare/node_modules/
cloudflare/.wrangler/
cloudflare/.dev.vars
cloudflare/data/india_produce_dictionary.json

# Local Netlify folder
.netlify
netlify-dist/

# Generated YouTube/debug dumps can contain page API keys and cookies.
tmp_*.html

# Local deploy-only secrets. Never commit browser cookies.
app/secrets/
hetzner/config.env

#hetzner
hetzner/extractor/.venv/
hetzner/extractor/node_modules/
